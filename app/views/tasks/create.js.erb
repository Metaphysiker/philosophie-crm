var div_id = "#tasks-<%= @task.taskable.id %>-<%= @task.taskable.class.to_s.downcase %>";

$( div_id ).fadeOut( "slow", function() {

    x = $("<%= escape_javascript( render "tasks/tasks_of_a_thing", parent: @task.taskable ) %>")

    $(div_id).empty().append(x).fadeIn("slow");

});
