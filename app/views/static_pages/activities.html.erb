<div class="container">
  <% @versions.each do |version| %>
    <%= version.inspect %>
    <p>
      <%= simple_format(version.object) unless version.object.nil? %>
    </p>
    <p>
      <%#= simple_format(version.changes) unless version.changes.nil? %>
    </p>
    <hr />
  <% end %>
</div>
